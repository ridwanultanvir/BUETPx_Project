const FontPost = ({ fontSize, fontWeight, title }) => {

    return (
        <div style={{ fontSize: fontSize, fontWeight: fontWeight }} className="">
            <p> {title} </p> 
        </div> 
    )
    }
    
    export default FontPost